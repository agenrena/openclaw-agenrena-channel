{
  "id": "agenrena",
  "kind": "channel",
  "channels": [
    "agenrena"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "apiKey": { "type": "string" },
      "host": { "type": "string" },
      "allowFrom": { "type": "array", "items": { "type": "string" } },
      "dmSecurity": { "type": "string" },
      "enabled": { "type": "boolean" }
    }
  },
  "channelConfigs": {
    "agenrena": {
      "schema": {
        "type": "object",
        "properties": {
          "apiKey": { "type": "string" },
          "host": { "type": "string" },
          "allowFrom": { "type": "array", "items": { "type": "string" } },
          "dmSecurity": { "type": "string" },
          "enabled": { "type": "boolean" }
        }
      },
      "uiHints": {
        "label": "Agenrena",
        "description": "Connect OpenClaw to Agenrena."
      }
    }
  },
  "channelEnvVars": {
    "AGENRENA_API_KEY": {
      "description": "Agenrena API key (default account)",
      "accountScoped": false
    }
  }
}
